name: ia_cardlink
description: "AppSDK CardLink Services."
version: 0.0.1
homepage:

environment:
  sdk: ">=3.10.0"
  flutter: ">=3.30.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  
flutter:
  plugin:
    platforms:
      android:
        package: de.ihreapotheken.appsdk.flutter.card_link
        pluginClass: IaSdkFlutterCardLink
      ios:
        pluginClass: IaSdkFlutterCardLink