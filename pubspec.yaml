name: appsdk_v2_flutter_plugin
description: "A new Flutter plugin project."
version: 0.0.1
homepage: https://ihreapotheken.de

environment:
  sdk: '>=3.10.0'
  flutter: '>=3.30.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  dartdoc: ^9.0.0

flutter:
  plugin:
    platforms:
      android:
        package: de.ihreapotheken.appsdk_v2_flutter_plugin
        pluginClass: IaSdkFlutter
      ios:
        pluginClass: IaSdkFlutter