name: ia_interface
description: "Interface object definitions for the ia.de AppSDK Flutter service."
version: 0.0.1
homepage: https://ihreapotheken.de/

environment:
  sdk: ">=3.10.0"
  flutter: ">=3.30.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter: